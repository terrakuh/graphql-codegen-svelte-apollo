# Codegen plugin for Svelte + Apollo 4

## Install

```shell
npm install -D github:terrakuh/graphql-codegen-svelte-apollo
```

## Developing

```shell
npm install
npm run watch

# To generate a test.
npm -w example run generate
```
